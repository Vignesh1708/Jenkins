pipeline {
    agent any
    stages {
        stage('deploy kubernetes cluster') {
            steps {
                sh 'echo $WORKSPACE'
                sh 'ansible-playbook ${WORKSPACE}/ansible-c/kubernetes-setup/k8s-setup.yaml'
            }
        }
      }
}