pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo $WORKSPACE'
                sh 'ansible-playbook k8s-setup.yaml'
            }
        }
      }
}