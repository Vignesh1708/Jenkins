pipeline {
    agent any
    stages {
        stage('deploy kubernetes cluster') {
            steps {
                sh 'cd $WORKSPACE/ansible-c/kubernetes-setup'
                sh 'ansible-playbook k8s-setup.yaml'
            }
        }
      }
}